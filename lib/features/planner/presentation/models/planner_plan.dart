class PlannerPlan {
  const PlannerPlan({
    required this.title,
    required this.quizCount,
    required this.detailQuizCount,
  });

  final String title;
  final int quizCount;
  final int detailQuizCount;
}
